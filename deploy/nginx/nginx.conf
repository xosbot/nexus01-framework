# NEXUS-01 — Nginx reverse proxy for navos.online
# Handles SSL termination, WebSocket upgrade, static asset caching

upstream nexus_backend {
    server nexus:8765;
    keepalive 32;
}

# Rate limiting zone
limit_req_zone $binary_remote_addr zone=nexus_limit:10m rate=30r/s;

server {
    listen 80;
    server_name navos.online www.navos.online;

    # ACME challenge for certbot
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect HTTP to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name navos.online www.navos.online;

    # SSL certificates (managed by certbot)
    ssl_certificate /etc/letsencrypt/live/navos.online/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/navos.online/privkey.pem;

    # SSL settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    # HSTS
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Max upload size (for RAG ingest, file uploads)
    client_max_body_size 50M;

    # Rate limiting
    limit_req zone=nexus_limit burst=20 nodelay;

    # Static assets — long cache
    location /assets/ {
        proxy_pass http://nexus_backend/assets/;
        proxy_cache_valid 200 7d;
        add_header Cache-Control "public, max-age=604800, immutable";
    }

    # WebSocket — /ws
    location /ws {
        proxy_pass http://nexus_backend/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket timeouts
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # Health check — no rate limit
    location /health {
        proxy_pass http://nexus_backend/health;
        limit_req off;
    }

    # Webhooks — no rate limit (external services need reliable delivery)
    location /webhooks/ {
        proxy_pass http://nexus_backend/webhooks/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        limit_req off;
    }

    # API + Dashboard — everything else
    location / {
        proxy_pass http://nexus_backend/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # API response compression
        proxy_buffering on;
        proxy_buffer_size 16k;
        proxy_buffers 8 16k;
    }
}
