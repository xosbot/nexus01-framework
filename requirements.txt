httpx>=0.27.0
beautifulsoup4>=4.12.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
python-telegram-bot>=21.0
pyyaml>=6.0
duckduckgo-search>=6.0
redis>=5.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
aiohttp>=3.9.0
pytest>=8.0
pytest-asyncio>=0.24
discord.py>=2.3.0

# Phase 3 — Deploy & Control
docker>=7.0.0
hcloud>=2.0.0
structlog>=24.0.0

# Phase 3 — OSINT Hardening (install on SSD Node server)
# crawl4ai>=0.4.0        # Self-hosted LLM-friendly scraper (pip install on server)
# sherlock-project>=0.15  # Username enumeration (pip install on server)
# theHarvester>=4.0       # Domain/email recon (pip install on server)
# holehe>=1.0             # Email account enumeration (pip install on server)
# onionsearch>=1.0        # Dark web search (Phase 4, requires Tor)
playwright>=1.44.0        # Browser automation for OSINT
